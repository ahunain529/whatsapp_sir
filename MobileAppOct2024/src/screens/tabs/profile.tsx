import {SafeAreaView, ScrollView} from 'react-native';

const ProfileScreen = () => {
  return (
    <SafeAreaView>
      <ScrollView></ScrollView>
    </SafeAreaView>
  );
};

export default ProfileScreen;
