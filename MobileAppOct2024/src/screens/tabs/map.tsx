import {SafeAreaView, ScrollView} from 'react-native';

const MapScreen = () => {
  return (
    <SafeAreaView>
      <ScrollView></ScrollView>
    </SafeAreaView>
  );
};

export default MapScreen;
